# 报告二：Vibe-Coding 工程化实践研究

## ——如何提高 AI 辅助编程的成功率

---

## 摘要

本报告基于"文档生成智能体"35 轮完整交互过程（约 502KB 原始日志），从工程化视角分析 Vibe-Coding 的实践特征，总结影响成功率的关键因素，并给出可操作的改进建议。研究发现，Vibe-Coding 的失败模式与传统开发截然不同——最大的风险不是技术实现错误，而是**环境一致性、AI 理解偏差、以及迭代中的状态漂移**。本报告提出了一套"防御性 Vibe-Coding"的工程实践框架。

---

## 一、交互数据统计

### 1.1 整体规模

| 指标 | 数值 |
|------|------|
| 总交互轮次 | 35 轮 |
| 用户消息数 | 35 条 |
| AI 工具调用总数 | 估算 300+ 次 |
| 涉及文件 | 7 个源码文件 + 1 个配置文件 |
| 项目总代码量 | 估算 4000+ 行 |
| 项目总时长 | 约 2 天（5/13 16:38 - 5/15 16:36） |

### 1.2 轮次分类

| 类型 | 轮数 | 占比 |
|------|------|------|
| 新功能开发 | 10 轮（R1, R2, R7, R8, R16, R22, R24, R28, R30, R32） | 29% |
| Bug 修复 | 8 轮（R3, R4, R5, R6, R12, R14, R19, R33） | 23% |
| 功能精化/调整 | 11 轮（R10, R11, R15, R17, R18, R20/21, R23, R26, R27, R29, R34） | 31% |
| 运维操作 | 2 轮（R9, R31） | 6% |
| 架构/安全 | 2 轮（R13, R35） | 6% |
| 误操作/重复 | 2 轮（R3 重复, R9 "继续执行"） | 6% |

**关键发现**：只有 29% 的轮次是"新功能开发"，**71% 的交互是修复、精化和调整**。这说明 Vibe-Coding 的主要成本不在"从零到一"，而在"从一到对"。

### 1.3 迭代代价：功能从提出到收敛所需的轮次

| 功能 | 首次提出 | 最终收敛 | 迭代轮数 |
|------|---------|---------|---------|
| 删除章节 | R4 | R5 | 2 |
| 章节移动排序 | R5 | R6 | 2 |
| 模型管理 | R2 | R6 | 5 |
| 目录层级控制 | R7 | R15 | 9 |
| 序号显示 | R18 | R21 | 4 |
| 多选批量生成 | R24 | R29 | 6 |
| 导出功能 | R32 | R34 | 3 |

**平均迭代轮次：4.4 轮**。目录层级控制以 9 轮迭代成为最耗时的功能，占项目总轮次的 26%。

---

## 二、失败模式分类与根因分析

### 2.1 失败模式分布

| 失败模式 | 出现次数 | 典型案例 |
|---------|---------|---------|
| **AI 理解偏差** | 8 次 | R5 删除仍不生效、R10 depth=2仍有3级目录、R20 过度删除序号徽章 |
| **环境/缓存不一致** | 5 次 | R9 浏览器缓存、R12 __pycache__缓存、R14 旧代码缓存、R31 主动清理 |
| **LLM 不遵守约束** | 5 次 | R10 depth约束无效、R12 depth=4只到3层、R15 LLM偷懒 |
| **边界条件遗漏** | 3 次 | R28 前一章未生成、R29 批量生成顺序、R33 中文文件名 |
| **状态漂移** | 2 次 | R34 全文导出受选择状态影响、R6 模型管理前后端状态不一致 |
| **Prompt 自相矛盾** | 2 次 | R10 system prompt写死"至少3层"与depth=2冲突、R15 "大部分分支"被利用 |

### 2.2 根因链分析

**案例1：目录层级控制（9轮迭代）**

```
用户要求 depth=2
  → R10：LLM仍生成3级目录
    → 根因A：Prompt中写死了"至少3层"与depth冲突（AI设计缺陷）
    → 根因B：LLM忽略user prompt中的depth约束（LLM不服从）
      → 修复：去掉system prompt中的矛盾约束 + user prompt更精确
        → R12：depth=2仍有3级目录
          → 根因C：__pycache__缓存了旧代码（环境不一致）
            → 修复：清理缓存
              → R13：截断策略丢失信息（AI方案缺陷）
                → 修复：将被截断内容合并到父节点
                  → R14：depth=2仍生成三级
                    → 根因D：_enforce_max_depth有bug（代码逻辑错误）
                      → 修复：简化递归逻辑
                        → R15：depth=4只到3级
                          → 根因E：Prompt中"大部分分支"给LLM偷懒借口（Prompt设计缺陷）
                            → 修复：改为"所有分支必须达到"
```

**教训**：一个看似简单的"depth=2"功能，涉及了5个不同层面的根因（Prompt冲突、LLM不服从、环境缓存、代码Bug、Prompt模糊），耗时9轮才收敛。**LLM 应用中，最简单的需求也可能触发多层问题叠加。**

**案例2：导出中文文件名 500 错误**

```
R32：用户要求导出功能 → AI实现，使用中文文件名
  → R33：导出报500错误
    → 根因：Content-Disposition header包含中文，Starlette用latin-1编码报错
      → 修复：RFC 5987 URL编码
```

**教训**：AI 在实现功能时**没有考虑非ASCII字符的编码问题**——这是一个典型的"正确但不够健壮"的实现。在中文环境下，编码问题是高频陷阱。

---

## 三、环境一致性问题

### 3.1 问题规模

在 35 轮交互中，**环境/缓存不一致问题出现了 5 次**，占比 14%。虽然不是最频繁的问题，但它导致了大量的无效调试时间——用户和 AI 都以为代码有问题，实际上是运行的不是最新代码。

### 3.2 缓存问题类型

| 类型 | 出现场景 | 根因 |
|------|---------|------|
| **浏览器缓存** | R9, R12 | 静态文件（HTML/JS/CSS）被浏览器缓存，Ctrl+F5才能加载新版本 |
| **Python `__pycache__`** | R14 | .pyc文件缓存了旧版本模块，Python import时使用了缓存而非源码 |
| **服务器未重载** | R14 | Uvicorn的reload机制在某些情况下未检测到文件变更 |

### 3.3 缓解策略

**已采用的策略**：
- R9：给静态文件URL添加版本号 `?v=2` 破坏缓存
- R31：手动清理 `__pycache__` 并重启服务

**推荐的最佳实践**：
1. **前端**：所有静态文件引用使用 hash 或版本号（`app.js?v=timestamp`）
2. **后端**：在关键 API 响应中包含版本/时间戳信息，方便确认代码是否最新
3. **开发环境**：使用 `uvicorn --reload` 并确保 reload 目录正确
4. **调试时**：每次修改后强制刷新浏览器（Ctrl+Shift+R），或在无痕窗口测试
5. **CI/CD 层面**：对 Python 包使用 `pip install -e .` 替代直接运行，避免 `__pycache__` 问题

---

## 四、AI 理解偏差与交互优化

### 4.1 理解偏差的类型

| 类型 | 描述 | 案例 |
|------|------|------|
| **过度实现** | AI 做了比要求更多的事，导致副作用 | R20：用户说"标题不要序号"，AI连蓝色徽章也删了 |
| **理解错误** | AI 理解的需求与用户意图不一致 | R34：全文导出仍受选择状态影响 |
| **方案缺陷** | AI 的实现方案存在设计缺陷 | R13：截断策略丢弃了有用信息 |
| **边界遗漏** | AI 没有考虑边界条件 | R33：中文文件名编码、R29：批量生成顺序 |

### 4.2 过度实现问题

**典型案例**：R18-21 的序号问题

```
R18：用户说"标题上不要有序号"
  → AI 删除了title文本中的序号（正确）
  → AI 同时删除了蓝色徽章显示（过度实现）
R19：用户说"我还是看到标题上有序号"
  → AI 进一步删除了所有序号显示
R20-21：用户说"蓝色徽章是要保留的"
  → AI 恢复了蓝色徽章
```

**根因**：AI 将"标题上不要有序号"理解为"一切序号显示都要去掉"，而用户的意图是"**title文本**中不要包含序号"。

**改进策略**：当需求涉及"去除某个元素"时，**明确指定去除的范围和保留的例外**。例如：
- ❌ "标题上不要有序号"
- ✅ "标题文字中不要有序号，但蓝色ID徽章保留"

### 4.3 理解偏差的修正效率

| 修正方式 | 轮次 | 效果 |
|---------|------|------|
| 重新描述需求 | R5, R14, R34 | 通常1轮解决 |
| 指出AI的错误 + 给出正确方案 | R13, R17, R23 | 通常1轮解决 |
| 仅指出错误 | R4, R6, R12, R19 | 可能需要2-3轮 |

**结论**：**修正理解偏差最有效的方式是"指出错误 + 给出正确方案"**。仅指出错误会让 AI 在多个可能的理解方向上试探，增加迭代次数。

---

## 五、Prompt 工程的迭代模式

### 5.1 Prompt 迭代统计

整个项目中，LLM 相关的 prompt 被修改了 **12 次**，分布在以下文件中：

| 文件 | 修改次数 | 主要内容 |
|------|---------|---------|
| `llm_client.py` (OUTLINE_SYSTEM_PROMPT) | 5 次 | 层级约束、示例优化、禁止套话 |
| `llm_client.py` (CHAPTER_SYSTEM_PROMPT) | 4 次 | 内容结构、子主题概述、禁止标题语法 |
| `llm_client.py` (CHAPTER_USER_TEMPLATE) | 3 次 | 格式重申、上下文传递 |
| `doc_generator.py` | 3 次 | 截断策略、标题清洗 |
| `llm_client.py` (GLOBAL_CONTEXT_*) | 1 次 | 新增全局上下文生成 |

### 5.2 Prompt 修改的触发因素

| 触发因素 | 次数 | 示例 |
|---------|------|------|
| LLM 输出不符合格式要求 | 5 次 | depth不匹配、内容中有子标题 |
| 用户提出新的内容规范 | 4 次 | "先总述再分述"、"不要套话" |
| 发现 prompt 自相矛盾 | 2 次 | system prompt与user prompt冲突 |
| 架构升级需要 | 1 次 | R28 上下文工程 |

### 5.3 Prompt 设计的关键经验

**经验一：具体的示例优于抽象的规则**

R10 的 prompt 只写了"层级深度由用户指定"，LLM 经常违反。R12 为每个 depth 值生成了**具体的 JSON 输出示例**，LLM 的遵守率大幅提升。

**经验二：消除一切模糊空间**

- ❌ "大部分分支应达到N层" → LLM 大量停在N-1层
- ✅ "所有分支必须达到N层" → LLM 基本遵守

**经验三：双重约束优于单重约束**

对于关键格式要求，同时在 system prompt 和 user prompt 中强调：
```
System: 禁止使用"本章旨在"、"本节旨在"等套话开头
User: 再次强调：直接进入实质内容，不要用"本章旨在"等套话
```

**经验四：后端校验是最后的防线**

无论 prompt 多精确，LLM 仍可能违规。`_enforce_max_depth()` 和 `_clean_titles()` 等**后端自动修正机制**是不可或缺的。

---

## 六、提高 Vibe-Coding 成功率的工程实践框架

### 6.1 交互策略层

#### 策略1：特异性分层

| 需求类型 | 推荐表达方式 | 示例 |
|---------|------------|------|
| 新功能 | 描述目标状态 + 约束条件 | "每个章节可以多选，选中后批量生成，按大纲顺序逐个迭代" |
| Bug修复 | 描述症状 + 复现路径 + 期望行为 | "点击删除按钮后章节未删除，点击X→点击回收站→章节仍在" |
| 功能精化 | 指出当前行为 + 期望行为 + 理由 | "并入时description中'已并入内容'改为'包括'，因为对生成无帮助" |

**统计数据支持**：精确方案的表达方式一次解决率约 80%，模糊方向仅约 33%。

#### 策略2：消除歧义的"保留/删除"模式

当要求"去除X"时，同时说明"保留Y"：
- ❌ "标题不要序号"
- ✅ "标题文字不要序号，蓝色ID徽章保留"

#### 策略3：边界条件预声明

在提出需求时，主动列出已知的边界条件：
- "第一章没有前一章"
- "前一章可能还没生成"
- "文件名可能是中文"

### 6.2 技术实践层

#### 实践1：代码热重载保障

- 使用 `uvicorn --reload` 并监控 reload 日志
- 前端静态文件加版本号 `?v={timestamp}`
- 关键修改后主动清理 `__pycache__`
- 在无痕窗口验证修改生效

#### 实践2：防御性 Prompt 设计

```python
# 1. 同时在 system 和 user prompt 中约束关键格式
# 2. 提供具体的输出示例而非抽象规则
# 3. 使用"禁止"而非"建议"
# 4. 后端校验 + 自动修正作为安全网
```

#### 实践3：LLM 输出的后端校验

```python
# 伪代码：三层防线
def generate_outline(depth):
    # 第一层：Prompt约束（软约束）
    result = llm.generate(prompt_with_depth(depth))
    # 第二层：后端校验（硬约束）
    actual_depth = get_actual_depth(result)
    if actual_depth > depth:
        # 第三层：自动修正
        result = enforce_max_depth(result, depth)
    return result
```

#### 实践4：编码问题的预防性处理

在中文环境下，凡是涉及 HTTP header、文件名、数据库存储的场景，**提前处理非 ASCII 字符编码**：
- HTTP header：使用 RFC 5987 编码
- 文件名：`urllib.parse.quote()`
- 配置文件中的敏感信息：base64 编码

### 6.3 流程管理层

#### 实践1：里程碑驱动的增量开发

不要试图一次性完成所有功能，而是按里程碑推进：

```
M1: 核心流程跑通（能从需求到导出）
M2: 交互可用（关键操作无阻断Bug）
M3: LLM输出可控（格式/深度/套话等基本可控）
M4: 上下文工程（全局一致性和连贯性）
M5: 产品化（体验、安全、边界处理）
```

#### 实践2：尽早引入上下文管道

上下文工程不应是事后的架构升级，而应在核心流程跑通后立即引入。最简版本：
1. 每次生成前，将完整大纲传给 LLM
2. 标注当前章节在大纲中的位置
3. 传递前一章的摘要

#### 实践3：修改后立即验证

每次 AI 修改代码后，用户应**立即验证**修改是否生效，而不是累积多个修改后再验证。这可以避免"修改堆叠"导致的调试困难。

---

## 七、Vibe-Coding 成功率的影响因子模型

基于 35 轮交互的实证数据，构建如下影响因子模型：

| 因子 | 影响方向 | 权重 | 说明 |
|------|---------|------|------|
| 需求表达特异性 | 正向 | ★★★★★ | 精确方案一次解决率 80% vs 模糊方向 33% |
| 上下文工程完备度 | 正向 | ★★★★ | R28 后内容质量显著提升 |
| Prompt 设计精确性 | 正向 | ★★★★ | 具体示例 + 双重约束 > 抽象规则 |
| 环境一致性 | 正向 | ★★★ | 缓存问题导致 14% 的交互是无效的 |
| AI 理解偏差频率 | 负向 | ★★★ | 23% 的轮次用于修复理解偏差 |
| LLM 不服从约束 | 负向 | ★★★ | 需要 Prompt + 后端校验双重防线 |
| 迭代轮次 | 负向 | ★★ | 迭代超过3轮后，用户耐心显著下降 |

---

## 八、结论

Vibe-Coding 的核心工程挑战不是"让 AI 写出正确的代码"，而是**在人类需求模糊性、LLM 输出不确定性、和环境复杂性三者之间建立稳健的交互闭环**。

提高成功率的三个核心原则：

1. **精确化需求表达**：从"我想要X"进化到"我要X，不要Y，边界是Z"。产品本身就是需求精确化的最佳工具——先用起来，再纠正。

2. **多层防御架构**：不信任 LLM 的输出格式，不信任 AI 的一次实现，不信任运行环境的一致性。每一层都是下一层的安全网。

3. **渐进式架构升级**：不要在第一天设计完美的架构，也不要永远在打补丁。在"能用"→"好用"→"可靠"→"安全"的阶梯上，每一步都基于前一步的真实问题。

**最终，Vibe-Coding 的工程化本质是：以快速的反馈循环代替缓慢的前置设计，以防御性的多层保障代替对单一环节的信任。**
