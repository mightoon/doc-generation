# 报告一：Vibe-Coding 构建应用的方法论分析

## ——基于"文档生成智能体"35轮交互的用户推进范式研究

---

## 摘要

本报告基于用户与 AI 编程助手（CodeBuddy）通过 35 轮交互构建"文档生成智能体"的完整过程，深入分析用户在应用构建中的推进范式。报告揭示了一个核心发现：用户并非按照"先设计后实现"的传统工程范式推进，而是遵循一种**"构建中思考"（Thinking Through Building）**的模式——通过不断使用产品来发现真实需求，通过修正输出来精确需求规格，通过认知跃迁来升级架构决策。这种模式的有效性依赖于 AI 编程助手"即时可运行"的交付能力，但也带来了一系列独特的方法论挑战。

---

## 一、推进范式的三层结构

### 1.1 显性推进：痛点驱动的问题解决

用户最表层的推进模式是**痛点驱动**——哪个环节正在阻碍使用，就优先解决哪个。整个 35 轮交互呈现出清晰的"流程顺序推进"特征：

| 阶段 | 轮次 | 核心焦点 | 推进动力 |
|------|------|---------|---------|
| 从零到一 | R1-3 | 核心流程搭建 | 产品愿景 |
| 交互修复 | R4-6 | 删除/移动/模型管理Bug | 操作不可用 |
| 大纲质量控制 | R7-15 | 目录层级精确性 | 生成质量不达标 |
| 大纲编辑完善 | R16-23 | 并入上级、序号、子标题注入 | 编辑流程不顺畅 |
| 内容生成质量 | R24-29 | 多选生成、上下文工程、prompt优化 | 生成内容质量差 |
| 产品完整化 | R30-35 | 一键生成、导出、安全 | 产品化需求 |

这种推进模式的优势是**始终聚焦于当前最大痛点**，避免了过度设计；劣势是**缺乏全局视角**，导致功能间的耦合和返工。

### 1.2 隐性推进：认知跃迁驱动的架构升级

在显性的痛点驱动之下，存在一条更深刻的**认知跃迁**线索。用户对产品的理解并非线性增长，而是在关键时刻发生"阶跃"：

**跃迁一：从"工具可用"到"交互正确"（R4-6）**

用户最初对产品的期望是"能跑起来就行"（R1），但第一次实际操作后立即发现了删除按钮不生效、无法移动章节等问题。这标志着用户从**概念验证**阶段进入了**交互验证**阶段。

**跃迁二：从"交互正确"到"LLM可控"（R10）**

R10 是一个关键转折点。当用户发现 depth=2 仍生成三级目录时，他没有简单地说"修一下"，而是提出了三个可能的根因：
> "是没传给大模型？还是提示词不清楚？还是大模型幻觉？"

这说明用户开始理解一个根本性的区别：**在 LLM 应用中，代码逻辑和 prompt 工程是两个独立的控制维度**。代码可以正确地传递参数，但 LLM 仍然可能不遵守——这是一个全新的工程挑战。

**跃迁三：从"修 prompt"到"构建上下文管道"（R28）**

R28 是整个项目最高层级的认知跃迁。用户系统性地提出了上下文工程方案：
1. 全局上下文摘要
2. 完整大纲树
3. 前一章摘要
4. 边界条件处理

这标志着用户从"头痛医头、脚痛医脚"的 prompt 微调，跃迁到了**架构级别的上下文管道设计**。用户理解了长文档生成的核心挑战不是单章质量，而是**全局一致性**。

**跃迁四：从"功能完整"到"产品级可用"（R30-35）**

一键生成全文、导出弹窗、API Key 加密——这些都不是核心算法问题，而是产品层面的体验和安全性问题。用户从"能不能用"切换到了"好不好用、安不安全"。

### 1.3 元推进：用户自身未意识到的推进范式

在显性和隐性推进之下，存在一个用户自身可能都没有意识到的**元模式**：

**模式一：从"描述想要什么"到"纠正不是什么"**

用户在 R1 描述了一个模糊的愿景（"友好的途径"、"详细多级目录大纲"），但随着项目推进，用户的表达方式从"我要X"逐渐转变为"X不对，应该是Y"：
- R17："已并入内容"→"包括"
- R23："包括"→"子标题"
- R27："总述的小标题没有必要"

这种**否定式精化**（通过纠正错误来逼近正确）比肯定式描述更高效，因为它利用了"可用产品"这个具体的参照物来发现差距。

**模式二：语义层面的绝对权威**

在所有决策中，用户覆盖 AI 的决策全部涉及**最终输出的语义**（文字措辞、信息保留、显示元素），而接受的决策全部涉及**交互机制和技术实现**（按钮类型、事件处理、数据存储格式）。

这揭示了一个隐含的决策原则：**凡是被最终用户感知到的东西，我亲自裁决；凡是不被感知的底层机制，我信任 AI。**

**模式三：特异性递增的反馈策略**

| 阶段 | 典型表达 | 特异性 |
|------|---------|--------|
| 早期 | "友好的途径"、"细致" | 模糊方向 |
| 中期 | "depth=2不应有三级目录" | 具体问题 |
| 后期 | "全局上下文+大纲树+前章摘要+边界处理" | 精确方案 |

用户在项目推进过程中**学会了更精确地表达需求**——这不是因为用户的表达能力提升了，而是因为**产品本身成为了需求规格的具体化工具**。用户在看到产品后，才能精确地知道自己要什么。

---

## 二、"构建中思考"范式的核心机制

### 2.1 产品即需求规格

传统开发中，需求规格文档先于产品存在。在 Vibe-Coding 中，**可运行的产品本身就是需求规格的载体**——用户通过使用产品来发现自己要什么，通过修正产品来精确自己要什么。

这导致了一个关键特征：**隐性需求的占比极高**。在 35 轮交互中，用户事先提及的需求不足最终功能的 40%，超过 60% 的功能是在使用过程中发现的：

| 隐性需求 | 发现轮次 | 触发场景 |
|---------|---------|---------|
| 文件上传解析 | R2 | 发现纯文本输入无法承载复杂需求 |
| 模型验证功能 | R2 | 配置模型后不确定是否可用 |
| 模型配置持久化 | R7 | 重启服务后需要重新配置 |
| 目录层级精确控制 | R8 | 生成大纲深度不可控 |
| 步骤回退/前进逻辑 | R8, R11 | 想在步骤间自由切换 |
| 章节移动排序 | R5 | 想调整大纲顺序 |
| 并入上级章节 | R16 | 想简化过度细分的大纲 |
| 确认大纲时的子标题注入 | R22 | 理解了大纲到内容的传递机制 |
| 全局上下文工程 | R28 | 内容生成偏离主题 |
| 批量生成顺序 | R29 | 发现多章生成从最后一章开始 |
| 一键生成全文 | R30 | 不想手动逐章选择 |
| 导出中文文件名 | R33 | 导出时报500错误 |
| API Key 加密 | R35 | 收尾时的安全意识 |

### 2.2 否定式精化优于肯定式描述

用户发现，**告诉 AI "不要什么"比"要什么"更有效**：

- R7："禁止在内容中创建子目录"比"内容应该怎样"更有效
- R29："不要用'本章旨在'这种句式"比"内容应该怎么开头"更有效
- R18："标题不要序号"比"标题应该是什么格式"更有效

这种模式的深层原因：**否定约束界定了输出的边界，而肯定描述留下了过多的解释空间**。LLM 的"创造力"在不加约束时是一种优势，但在需要精确控制输出格式时变成了一种干扰。

### 2.3 渐进式架构升级

用户的架构决策不是前置的，而是在问题积累到一定程度后"爆发式"提出的：

- R28 的上下文工程是对 R7-27 所有内容生成质量问题的系统性回应
- R32 的导出设计是对 R30-31 生成完成后的自然延伸
- R35 的安全加固是对整个项目工程化不足的收尾修正

这意味着：**Vibe-Coding 中的架构决策是被问题倒逼的，而非主动规划的。** 优点是避免了过度设计，缺点是可能需要重构已实现的功能。

---

## 三、LLM 应用构建的特殊方法论

### 3.1 双重控制维度：代码逻辑 vs Prompt 工程

传统软件只有代码逻辑一个控制维度。LLM 应用有两个：**代码逻辑**保证参数正确传递，**Prompt 工程**保证 LLM 正确理解意图。

用户在 R10 的三连问——"是没传给大模型？还是提示词不清楚？还是大模型幻觉？"——精确地概括了 LLM 应用的调试困境：**同一个症状（输出不符合预期）可能来自三个不同的根因。**

这导致了一种独特的调试模式：**多层防御**。当 prompt 无法完全约束 LLM 时，用户和 AI 不得不在后端增加校验和截断作为安全网（R10-15 的深度控制），形成"prompt 约束 + 后端校验"的双重防线。

### 3.2 LLM 的"偷懒"特性与 Prompt 设计

用户在 R15 发现了一个关键现象：**当 prompt 中存在任何模糊空间时，LLM 会倾向于"偷懒"——生成比要求更浅的结构。**

- R15：depth=4 时，prompt 写"大部分分支应达到4层"，LLM 就让大量分支停在3层
- 修复方式：将"大部分"改为"所有"，将示例中的浅层分支全部替换为完整深度的结构

**教训**：Prompt 中的每一个模糊词（"大部分"、"尽量"、"建议"）都会被 LLM 利用为偷懒的借口。精确的 prompt 要求**消除一切弹性**，必要时提供**具体的输出示例**而非抽象描述。

### 3.3 上下文工程：从 Prompt 微调到管道设计

R28 的上下文工程是整个项目的架构核心。它揭示了一个深层认知：

**长文档生成的核心挑战不是"如何写好一章"，而是"如何让每一章都知道其他章的存在"。**

这导致了一种工程模式转变：从优化单个 prompt 到**构建上下文管道**——
一个系统化的信息传递机制，确保每次 LLM 调用都能获得足够的全局和局部上下文。

上下文管道的四个层次：
1. **全局摘要**：整个文档的核心主题和方向
2. **完整大纲树**：标注当前章节位置
3. **前一章摘要**：保证上下文连贯
4. **边界处理**：第一章无前序、前章未生成等

---

## 四、对 Vibe-Coding 实践的方法论建议

### 4.1 接受"构建中思考"，但设置里程碑

不要试图在项目开始时做完整的需求分析——这是反人性的，因为你确实不知道自己要什么。但应该设置阶段性的里程碑：

- **M1**：核心流程跑通（收集→大纲→生成→导出）
- **M2**：交互可用（关键操作无阻断性Bug）
- **M3**：质量达标（LLM 输出符合基本格式要求）
- **M4**：上下文工程（全局一致性和连贯性）
- **M5**：产品化（体验优化、安全加固）

### 4.2 用"否定式精化"代替"肯定式描述"

告诉 AI "不要什么"比"要什么"更有效。尤其是：
- 禁止特定的输出格式（"不要用#标题语法"）
- 禁止特定的措辞（"不要用'本章旨在'"）
- 禁止特定的行为（"不要创建子目录"）

### 4.3 在 Prompt 和代码之间建立双重防线

不要只依赖 Prompt 来约束 LLM 的输出——Prompt 是软约束，LLM 可能违反。在代码层面增加：
- 输出校验（深度检查、格式验证）
- 自动修正（截断超层级节点、清除序号前缀）
- 日志验证（打印实际深度 vs 要求深度）

### 4.4 早期建立上下文管道

R28 的上下文工程应该在 M2 阶段就引入，而不是等到 M3 才发现内容质量有问题。一旦产品流程跑通，就应该为 LLM 调用建立基本的上下文传递机制，哪怕初始版本很简单。

### 4.5 区分"感知层"和"机制层"的决策

- **感知层**（文字措辞、UI元素、信息展示）：用户必须亲自裁决
- **机制层**（技术实现、数据结构、交互方式）：可以信任 AI 的判断

这种区分可以减少不必要的来回沟通——用户只需关注自己真正关心的输出层面。

---

## 五、结论

Vibe-Coding 不是一个"不规划就动手"的懒人方法，而是一种**以可运行产品为媒介的需求发现和精确化过程**。它的核心假设是：**人无法在空白纸上精确描述自己要什么，但可以在看到产品后精确地知道自己不要什么。**

这种模式的有效性高度依赖 AI 编程助手的两个能力：
1. **即时可运行的交付**——让用户能"看到"产品
2. **精确理解否定式反馈**——让用户能"纠正"产品

最终，Vibe-Coding 的方法论可以概括为一句话：**先构建，再思考；先否定，再精确；先可用，再完善。**
